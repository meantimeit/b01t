// encryptMessage :: (string -> string -> func) -> string -> string -> callback
function encryptMessage(encrypter, encryptionKey, unencryptedMessage, encryptionCompleted) {
  encrypter(encryptionKey, unencryptedMessage, encryptionCompleted);
}

module.exports = encryptMessage;
