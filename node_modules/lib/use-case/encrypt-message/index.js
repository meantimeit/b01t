function encryptMessage(encryptable, encryptionKeyString, unencryptedMessage, encryptionCompleted) {
  // Call the encryptable interface with the key, message and callback
  encryptable(encryptionKeyString, unencryptedMessage, encryptionCompleted);
}

module.exports = encryptMessage;
