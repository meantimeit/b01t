function createPair(ciphergenable, options, done) {
  // Call the ciphergenable interface, passing in a user, passphrase and callback
  ciphergenable(options.userId, options.passphrase, done);
}

module.exports = createPair;
