function encrypt(crypto, encryptionKeyString, unencryptedMessage, encryptionCompleted) {
  // Call the encryptable interface with the key, message and callback
  crypto.encrypt(encryptionKeyString, unencryptedMessage, encryptionCompleted);
}

module.exports = encrypt;
